rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open read/write for now — add auth later when sharing
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
